// src/app/page.tsx

export default function Home() {
  return (
    <main
      style={{
        minHeight: '100vh',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        color: 'var(--color-text-primary)',
        fontSize: 'var(--font-size-lg)',
      }}
    >
      ⚙️ Atomity Challenge — Hour 1 complete
    </main>
  );
}